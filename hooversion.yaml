branches:
  - main
packages:
  - name: hooversion
    path: .
    type: version-file
    manifest: VERSION
hooks:
  afterVersion:
    - scripts/sync-action-versions.sh "$(tr -d '[:space:]' < VERSION)"
github:
  releases: true
